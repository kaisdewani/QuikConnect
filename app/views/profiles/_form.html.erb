<%= simple_form_for @profile do |f| %>
  <%= f.input :first_name, label: "First Name"%>
  <%= f.input :last_name, label: "Last Name"%>
  <%= f.input :personal_email, email: "Email Address"%>
  <%= f.button :submit %>
<% end %>